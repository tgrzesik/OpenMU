apiVersion: v1
kind: Service
metadata:
  name: {{ include "openmu-connect.fullname" . }}
  labels:
    {{- include "openmu-connect.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openmu-connect.selectorLabels" . | nindent 4 }}
