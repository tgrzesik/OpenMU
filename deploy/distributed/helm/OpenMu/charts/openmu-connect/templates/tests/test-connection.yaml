apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openmu-connect.fullname" . }}-test-connection"
  labels:
    {{- include "openmu-connect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openmu-connect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
